export class Player {
    id: number
    name: string
    nameAudioFile: string

    constructor( id: number, name: string, nameAudioFile: string)
    {
        this.id=id;
        this.name=name;
        this.nameAudioFile=nameAudioFile;
    }

}